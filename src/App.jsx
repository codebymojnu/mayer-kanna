import AllCreditor from "./components/AllCreditor";

function App() {
  const theme = {
    backgroundColor: "black",
    color: "pink",
  };

  return (
    <div style={theme}>
      <AllCreditor />
    </div>
  );
}

export default App;
